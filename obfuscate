-- SimpleVertex.lua
local Vertex = {}

function Vertex.Execute(code)
    -- Decode XOR
    local decoded = ""
    for i = 1, #code do
        decoded = decoded .. string.char(string.byte(code, i) ~ 55)
    end
    
    -- Execute
    getfenv().loadstring(decoded)()
end

return Vertex
