-- SimpleVertex.lua (Fixed)
local Vertex = {}

function Vertex.Execute(code)
    local decoded = ""
    for i = 1, #code do
        decoded = decoded .. string.char(string.byte(code, i) ~ 77)
    end
    getfenv().loadstring(decoded)()
end

return Vertex
