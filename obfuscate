-- SimpleRayObf.lua
return {
    Execute = function(code)
        local decoded = ""
        for i = 1, #code do
            decoded = decoded .. string.char(string.byte(code, i) ~ 66)
        end
        loadstring(decoded)()
    end
}
